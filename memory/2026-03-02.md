# 2026-03-02

- Anh nhắc em quét lại workspace — em đã đọc đủ `memory/` (2026-02-28, 2026-03-01, mentions-*).
- Anh yêu cầu cập nhật "những gì cần làm":
  - **Đã tạo** `MEMORY.md`: nhiệm vụ định kỳ (check mention 8h & 20h), project/focus (Radio Global, landing, apps CDev), quy ước, mục đã xong.
  - **Đã cập nhật** `USER.md` (Context): projects, đang làm, quy ước.
  - **Đã chỉnh** `HEARTBEAT.md`: ghi rõ task 2026-02-28 đã xong, bỏ checkbox theo ngày cố định.
- **Script Windows startup — OpenClaw gateway:**
  - `scripts/start-openclaw-gateway.bat`: cd tới `..\node_modules\openclaw-atom` (so với scripts\), chạy `node openclaw.mjs gateway --allow-unconfigured`.
  - `scripts/add-openclaw-to-startup.ps1`: tạo shortcut "OpenClaw Gateway" trong Startup folder (chạy minimized khi đăng nhập). Chạy 1 lần: `powershell -ExecutionPolicy Bypass -File scripts\add-openclaw-to-startup.ps1`.
